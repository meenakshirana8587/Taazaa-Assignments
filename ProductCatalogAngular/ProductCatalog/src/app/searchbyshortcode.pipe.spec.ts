import { SearchbyshortcodePipe } from './searchbyshortcode.pipe';

describe('SearchbyshortcodePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchbyshortcodePipe();
    expect(pipe).toBeTruthy();
  });
});
