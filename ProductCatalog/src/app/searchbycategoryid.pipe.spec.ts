import { SearchbycategoryidPipe } from './searchbycategoryid.pipe';

describe('SearchbycategoryidPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchbycategoryidPipe();
    expect(pipe).toBeTruthy();
  });
});
